<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <span class="navbar-brand">DoorApplication</span>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link active", "aria-current": "page" %>
        </li>
        <li class="nav-item">
          <% if user_signed_in? %>
            <%= link_to "Profile",  edit_user_registration_path, class: "nav-link active", "aria-current": "page" %>
          <% else %>
            <%= link_to "Profile",  new_user_session_path, class: "nav-link active", "aria-current": "page" %>
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to "Rooms", rooms_path, class: "nav-link active", "aria-current": "page" %>
        </li>
        <li class="nav-item">
          <%= link_to "Card", cards_path, class: "nav-link active", "aria-current": "page" %>
        </li>
      </ul>
      <ul class="nav navbar-nav ml-auto">
        <li class="nav-item">
          <% if user_signed_in? %>
            <%= link_to "Logout", users_sign_out_path, :method => :delete, class: "nav-link active", "aria-current": "page" %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>